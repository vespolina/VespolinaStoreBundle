<div class="row">
    <div class="span12 offset1">
        <div class="row">
            <div class="span4">
                <a href="{{ url('vespolina_store_products', { 'taxonomyTerm': 'all' }) }}">
                    <div>All</div>
                </a>
            </div>
        {% for term in terms %}
            <div class="span4">
                <a href="{{ url('vespolina_store_products', { 'taxonomyTerm': term.path }) }}">
                    <div>{{ term.name }}</div>
                </a>
            </div>
        {% else %}

        {% endfor %}
        </div>
    </div>
</div>
