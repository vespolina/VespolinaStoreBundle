<div class="row">
    <div class="span12 offset1">
        <div class="row">
        {% for term in terms %}
            <div class="span4">
                <a href="{{ url('vespolina_store_products', { 'slug': term.code }) }}">
                    <div>{{ term.name }}</div>
                </a>
            </div>
        {% else %}

        {% endfor %}
        </div>
    </div>
</div>
