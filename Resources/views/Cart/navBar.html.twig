{% if cart.isEmpty %}
<li class="dropdown">
    <a href="{{ path('vespolina_cart_show')}}"><i class="icon-shopping-cart"></i>Cart <span class="badge">0</span></a>
</li>
{% else %}
<li class="v-nav-cart">
    <a href="{{ path('vespolina_cart_show')}}" ><i class="icon-shopping-cart icon-white"></i>Cart </a>
</li>
<li class="v-nav-cart dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="badge">{{ cart.items.count }}<b class="caret"></b></a>
    <ul class="dropdown-menu">
    {% for item in cart.items %}
        <li><a href="{{ path('vespolina_store_product_detail', { 'slug': item.cartableItem.slug}) }}">({{ item.quantity }})  {{ item.name }}  -  {{ item.totalPrice | price_format(store.defaultCurrency) }}</a>
    {% endfor %}
        <li class="divider"></li>
        <li>
            <a class="v-nav-checkout" href="{{ path('vespolina_store_cart_checkout') }}">
                Grand Total {{ totalPrice | price_format(store.defaultCurrency) }}<br/>
                <button class="btn btn-primary">Checkout Now</button>
            </a>
        <li>
    </ul>
</li>
{% endif %}
